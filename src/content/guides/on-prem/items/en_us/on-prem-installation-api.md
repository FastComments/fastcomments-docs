The API can be accessed just like the regular SaaS product in that you would log in to the on-prem dashboard to create API keys and use those keys to access the API. The API
endpoints are the same on-prem as they are for the SaaS product.