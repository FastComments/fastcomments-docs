En komplet showcase-app ligger i `examples/example-showcase/`. Den spejler React-showcasen og dækker alle
widgets samt almindelige flows (mørk tilstand, paginering, SSO, callbacks).

```bash
cd examples/example-showcase
npm install
npm run dev
```