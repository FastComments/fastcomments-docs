Полноценное демонстрационное приложение находится в `examples/example-showcase/`. Оно повторяет демонстрацию для React и охватывает каждый
виджет, а также распространённые сценарии (тёмная тема, пагинация, SSO, обратные вызовы).

```bash
cd examples/example-showcase
npm install
npm run dev
```