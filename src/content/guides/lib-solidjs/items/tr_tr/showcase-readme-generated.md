Tam bir örnek uygulama `examples/example-showcase/` dizininde yer alır. Bu, React örnek uygulamasını yansıtır ve her
widget ile yaygın akışları (karanlık mod, sayfalandırma, SSO, callbacks) kapsar.

```bash
cd examples/example-showcase
npm install
npm run dev
```