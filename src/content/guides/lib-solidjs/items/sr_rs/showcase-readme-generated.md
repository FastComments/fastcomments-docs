Пуна апликација за приказ налази се у `examples/example-showcase/`. Она прати React showcase и покрива сваки
видџет плус уобичајене токове (тамни режим, страничење, SSO, повратни позиви).

```bash
cd examples/example-showcase
npm install
npm run dev
```