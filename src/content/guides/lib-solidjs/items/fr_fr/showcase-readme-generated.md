Une application de démonstration complète se trouve dans `examples/example-showcase/`. Elle reflète le showcase React et couvre chaque
widget ainsi que les flux courants (mode sombre, pagination, SSO, callbacks).

```bash
cd examples/example-showcase
npm install
npm run dev
```