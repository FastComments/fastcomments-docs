Una aplicación de muestra completa se encuentra en `examples/example-showcase/`. Refleja la demostración de React y cubre cada
widget además de flujos comunes (modo oscuro, paginación, SSO, callbacks).

```bash
cd examples/example-showcase
npm install
npm run dev
```