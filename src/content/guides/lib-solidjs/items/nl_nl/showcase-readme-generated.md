Een volledige showcase-app bevindt zich in `examples/example-showcase/`. Het weerspiegelt de React showcase en behandelt elke
widget plus veelvoorkomende flows (donkere modus, paginering, SSO, callbacks).

```bash
cd examples/example-showcase
npm install
npm run dev
```