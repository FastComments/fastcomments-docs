Un'applicazione completa di esempio si trova in `examples/example-showcase/`. Riproduce lo showcase di React e copre ogni
widget oltre ai flussi comuni (modalità scura, paginazione, SSO, callback).

```bash
cd examples/example-showcase
npm install
npm run dev
```