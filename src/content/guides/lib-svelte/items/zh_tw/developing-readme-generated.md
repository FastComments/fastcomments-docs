在您使用 `npm install`（或 `pnpm install` 或 `yarn`）安裝相依套件之後，啟動開發伺服器：

```bash
npm run dev

# or start the server and open the app in a new browser tab
npm run dev -- --open
```

`src/lib` 內的所有內容屬於函式庫，`src/routes` 內的所有內容則是示範程式。