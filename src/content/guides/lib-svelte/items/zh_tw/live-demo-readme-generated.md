在 <https://fastcomments.com/commenting-system-for-svelte> 線上即時試用所有元件。

一套針對 FastComments 的 Svelte 元件與封裝器。 `src/routes` 包含可執行的範例 - 只需執行：

    npm install
    npm run dev

接著你就可以查看所有元件並進行編輯 - 例如填入你自己的 tenant id 或嘗試不同的設定。

專案使用 TypeScript，讓你可以檢查每個元件的設定物件，以查看它們可接受哪些選項。