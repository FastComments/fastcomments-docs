Depois de instalar as dependências com `npm install` (ou `pnpm install` ou `yarn`), inicie um servidor de desenvolvimento:

```bash
npm run dev

# or start the server and open the app in a new browser tab
npm run dev -- --open
```

Tudo dentro de `src/lib` faz parte da biblioteca, tudo dentro de `src/routes` é a demonstração.