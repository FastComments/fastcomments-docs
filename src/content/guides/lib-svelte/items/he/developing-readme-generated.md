לאחר התקנת התלויות עם `npm install` (או `pnpm install` או `yarn`), הפעל שרת פיתוח:

```bash
npm run dev

# או להפעיל את השרת ולפתוח את האפליקציה בכרטיסייה חדשה בדפדפן
npm run dev -- --open
```

הכל בתוך `src/lib` הוא חלק מהספרייה, והכל בתוך `src/routes` הוא תצוגת הדגמה.