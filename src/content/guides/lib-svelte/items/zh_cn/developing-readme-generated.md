在使用 `npm install`（或 `pnpm install` 或 `yarn`）安装依赖后，启动开发服务器：

```bash
npm run dev

# or start the server and open the app in a new browser tab
npm run dev -- --open
```

`src/lib` 中的所有内容都是库的一部分，`src/routes` 中的所有内容是示例展示。