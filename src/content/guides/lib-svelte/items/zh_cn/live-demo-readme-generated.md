---
尝试在 <https://fastcomments.com/commenting-system-for-svelte/> 实时查看每个小部件。

一套用于 FastComments 的 Svelte 小部件和包装器。`src/routes` 包含可运行的示例——只需运行：

    npm install
    npm run dev

然后您可以查看所有小部件并进行编辑——例如放入您自己的租户 ID 或尝试不同的配置。

已使用 TypeScript，这样您可以检查每个小部件的配置对象，以了解它们接受哪些选项。
---