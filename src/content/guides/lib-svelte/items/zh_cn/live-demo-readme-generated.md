在 <https://fastcomments.com/commenting-system-for-svelte> 上实时试用每个小部件。

一套用于 FastComments 的 Svelte 小部件和封装。`src/routes` 包含可运行的示例 - 只需运行：

    npm install
    npm run dev

然后你可以查看所有小部件并编辑它们 - 例如将你自己的 tenant id 放入或尝试不同的配置。

使用了 TypeScript，以便你可以检查每个小部件的 config 对象，查看它们接受哪些选项。