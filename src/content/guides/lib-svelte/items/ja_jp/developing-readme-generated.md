---
依存関係を `npm install`（または `pnpm install` または `yarn`）でインストールしたら、開発サーバーを起動します:

```bash
npm run dev

# またはサーバーを起動してアプリを新しいブラウザタブで開く
npm run dev -- --open
```

`src/lib` 内のすべてはライブラリの一部で、`src/routes` 内のすべてはショーケースです。
---