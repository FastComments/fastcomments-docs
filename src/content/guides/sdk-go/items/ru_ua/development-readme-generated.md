Чтобы обновить сгенерированный клиент из спецификации OpenAPI:

```bash
./update.sh
```

Это скачает последнюю спецификацию OpenAPI с запущенного сервера FastComments (или использует локальную копию) и пересоздаст клиентский код.