Για να ενημερώσετε τον generated client από το OpenAPI spec:

```bash
./update.sh
```

Αυτό θα κατεβάσει την πιο πρόσφατη OpenAPI spec από έναν ενεργό FastComments server (ή θα χρησιμοποιήσει ένα τοπικό αντίγραφο) και θα ξαναπαράγει τον κώδικα του client.