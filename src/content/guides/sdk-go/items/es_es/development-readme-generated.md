Para actualizar el cliente generado a partir de la especificación OpenAPI:

```bash
./update.sh
```

Esto descargará la última especificación OpenAPI de un servidor FastComments en funcionamiento (o usará una copia local) y regenerará el código del cliente.