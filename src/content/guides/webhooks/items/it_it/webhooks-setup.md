Segui gli stessi passaggi per `localhost` che useresti per l'ambiente di produzione. Assicurati di avere configurati i domini di produzione e gli API Secrets.

Per prima cosa, naviga su [Webhooks admin](https://fastcomments.com/auth/my-account/manage-data/webhooks). È accessibile tramite Manage Data -> Webhooks.

La pagina di configurazione appare come segue:

[app-screenshot-start url='/auth/my-account/manage-data/webhooks'; selector = '.content'; title='Webhooks Configuration' app-screenshot-end]

In questa pagina puoi specificare endpoint per ogni tipo di evento di commento.

Per ogni tipo di evento, fai clic su Send Test Payload per verificare che l'integrazione sia stata configurata correttamente. Consulta la sezione successiva, "Testing", per i dettagli.