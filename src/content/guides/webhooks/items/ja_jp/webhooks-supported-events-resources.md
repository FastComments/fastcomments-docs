---
FastComments は Comment リソースに対してのみ Webhook をサポートしています。

コメントの作成、削除、更新に対する Webhook をサポートしています。

これらはすべてシステム内で別々のイベントとみなされ、Webhook イベントのセマンティクス  
と構造が異なります。

任意の数のエンドポイントが同じイベントを購読できます: ドメインごとに 1 つの Webhook をダッシュボードで設定でき、  
さらに API を通じて購読を作成できます（API での Webhook 管理を参照）。