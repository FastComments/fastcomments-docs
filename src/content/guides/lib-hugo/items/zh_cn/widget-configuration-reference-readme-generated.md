---
有关小部件选项的完整列表，请参阅 [FastComments 自定义文档](https://docs.fastcomments.com/guide-customizations-and-configuration.html)。两个小部件采用一个没有默认值的必需选项：

- `fastcomments-collab-chat` 和 `fastcomments-image-chat` 需要 `target`，这是用于附加到元素的 CSS 选择器。
- `fastcomments-user-activity-feed` 需要 `userId`。
---