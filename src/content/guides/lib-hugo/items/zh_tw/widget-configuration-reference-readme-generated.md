欲查看完整的小工具選項清單，請參閱 [FastComments 自訂與設定文件](https://docs.fastcomments.com/guide-customizations-and-configuration.html)。有兩個小工具需要一個沒有預設值的必要選項：

- `fastcomments-collab-chat` and `fastcomments-image-chat` 需要 `target`，這是用來指定要附加到元素的 CSS 選擇器。
- `fastcomments-user-activity-feed` 需要 `userId`。