위젯 옵션 전체 목록은 [FastComments 커스터마이제이션 문서](https://docs.fastcomments.com/guide-customizations-and-configuration.html)를 참조하세요. 두 위젯은 기본값이 없는 필수 옵션을 필요로 합니다:

- `fastcomments-collab-chat` 및 `fastcomments-image-chat`는 연결할 요소의 CSS 선택자인 `target`을 필요로 합니다.
- `fastcomments-user-activity-feed`는 `userId`가 필요합니다.