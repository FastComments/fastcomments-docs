כדי לראות כל ווידג'ט וזרימה שרצים מקומית מול ה-tenant הציבורי `demo`, שכפל את המאגר והרץ:

```bash
npm install
npm run dev
```

נקודת הכניסה של התצוגה נמצאת ב`src/App.vue` וב`index.html` — כל ווידג'ט/זרימה מוצגים באותה אפליקציה שניתן להעתיק אל פרויקט Vue 3 משלך.