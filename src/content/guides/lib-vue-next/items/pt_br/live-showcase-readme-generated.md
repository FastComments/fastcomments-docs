Para ver todos os widgets e fluxos sendo executados localmente contra o tenant público `demo`, clone o repositório e execute:

```bash
npm install
npm run dev
```

O ponto de entrada da demonstração fica em `src/App.vue` e `index.html` — cada widget/fluxo é renderizado no mesmo app que você pode copiar para o seu próprio projeto Vue 3.