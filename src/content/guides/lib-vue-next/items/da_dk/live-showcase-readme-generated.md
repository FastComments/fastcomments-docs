For at se alle widgets og flows køre lokalt mod den offentlige `demo`-tenant, klon repoet og kør:

```bash
npm install
npm run dev
```

Showcasens indgangspunkt ligger i `src/App.vue` og `index.html` — hver widget/flow gengives i den samme app, som du kan kopiere ind i dit eget Vue 3-projekt.