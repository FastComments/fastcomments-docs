Per vedere tutti i widget e i flow in esecuzione localmente contro il tenant pubblico `demo`, clona il repository ed esegui:

```bash
npm install
npm run dev
```

Il punto di ingresso della showcase si trova in `src/App.vue` e `index.html` — ogni widget/flow viene renderizzato nella stessa app che puoi copiare nel tuo progetto Vue 3.