Para ver cada widget y flujo ejecutándose localmente contra el tenant público `demo`, clona el repositorio y ejecuta:

```bash
npm install
npm run dev
```

El punto de entrada de la muestra se encuentra en `src/App.vue` y `index.html` — cada widget/flujo se renderiza en la misma aplicación que puedes copiar en tu propio proyecto de Vue 3.