---
Чтобы обновить сгенерированный клиент из спецификации OpenAPI:

```bash
./update.sh
```

Это загрузит последнюю спецификацию OpenAPI с работающего сервера FastComments (или использует локальную копию) и заново сгенерирует клиентский код.
---