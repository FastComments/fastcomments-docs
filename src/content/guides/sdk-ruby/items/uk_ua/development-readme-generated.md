Щоб оновити згенерований клієнт на основі специфікації OpenAPI:

```bash
./update.sh
```

Ця команда завантажить останню специфікацію OpenAPI з працюючого сервера FastComments (або використає локальну копію) та згенерує код клієнта заново.