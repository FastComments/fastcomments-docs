---
Todas as bibliotecas front-end oficiais do FastComments para React, Angular, Vue, e assim por diante, aceitam o parâmetro de configuração `customCSS`
e são wrappers em torno da biblioteca vanilla JS com a mesma estrutura HTML.

---