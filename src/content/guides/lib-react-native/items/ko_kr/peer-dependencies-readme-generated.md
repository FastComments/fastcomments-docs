`fastcomments-react-native`는 `react-native-webview`를 필요로 합니다. 따라서 `react-native-webview`의 설치 절차를 따라야 합니다.

### 계정 지역 (주의: EU 고객)

계정이 EU에 있는 경우 위젯 구성에서 `region = 'eu'`로 설정하세요. 그렇지 않으면 `region`을 정의할 필요가 없습니다.