```
fastcomments-dart/
  client/        сгенерированный dart клиент (не редактировать вручную)
  sso/           ручные помощники токенов SSO
  openapi.json   зафиксированная копия спецификации, использованной для генерации
  config.json    конфигурация openapi-generator
  update.py      перегенерировать клиент из спецификации
```