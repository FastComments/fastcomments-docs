```
fastcomments-dart/
  client/        сгенерированный dart‑клиент (не редактировать вручную)
  sso/           ручные вспомогательные функции SSO‑токенов
  openapi.json   закоммиченная копия спецификации, используемой для генерации
  config.json    конфигурация openapi‑generator
  update.py      восстановить клиент из спецификации
```