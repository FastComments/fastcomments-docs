---
Generator prevzame specifikacijo iz lokalno zagnanega strežnika FastComments (`http://localhost:3001/js/swagger.json`), če je na voljo, sicer se vrne na potrjeno datoteko `openapi.json`.

```bash
python3 update.py
```

Zahteva `node`/`npx` (za `@openapitools/openapi-generator-cli`) in Java.
---