Para ver todos los widgets y flujos ejecutándose localmente contra el tenant público `demo`, clona el repositorio y ejecuta:

```bash
npm install
npm run serve
```

Las demos individuales también tienen sus propios puntos de entrada (`npm run serve-collab-chat`, `serve-live-chat`, `serve-image-chat`, `serve-pagination`, `serve-toggle-dark-mode`) en `dev/`.