---
O autocomplete de `@mentions` pode ser pré-populado com os usuários da sua plataforma ou aplicação usando a [API](/guide-api.html).

Ao usar a API, é possível popular tanto SSO quanto Tenant Users. Ambos aparecerão no autocomplete de `@mentions` dependendo da configuração do grupo.

---