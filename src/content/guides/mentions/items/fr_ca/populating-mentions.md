L'autocomplétion `@mentions` peut être préremplie avec les utilisateurs de votre plateforme ou application en utilisant l'[API](/guide-api.html).

En utilisant l'API, il est possible de préremplir à la fois les utilisateurs SSO et les Tenant Users. Les deux apparaîtront dans l'autocomplétion `@mentions` en fonction de la configuration du groupe.