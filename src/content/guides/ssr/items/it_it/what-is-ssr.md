SSR, o Rendering lato server, è un meccanismo e un insieme di convenzioni in cui un'applicazione o un contenuto viene renderizzato con
lavoro minimo da parte del client.


Con SSR, il client non esegue JavaScript. Invece, il client visualizza HTML/CSS che è stato "renderizzato" dal server.