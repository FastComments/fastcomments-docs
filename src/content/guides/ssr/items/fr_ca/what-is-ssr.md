---
SSR, ou rendu côté serveur (Server-Side-Rendering), est un mécanisme et un ensemble de conventions où une application ou un contenu est rendu avec
un travail minimal du côté client.


Avec le SSR, le client n'exécute pas de JavaScript. Au lieu de cela, le client affiche du HTML/CSS qui a été "rendu" par le serveur.

---