SSR, o renderizado en el servidor, es un mecanismo y un conjunto de convenciones donde una aplicación o contenido se renderiza con
un esfuerzo mínimo por parte del cliente.


Con SSR, el cliente no ejecuta JavaScript. En su lugar, el cliente renderiza HTML/CSS que fue "renderizado" por el servidor.