SSR, odnosno Server-Side-Rendering, je mehanizam i skup konvencija u kojima se aplikacija ili sadržaj renderira s
minimalnim radom klijenta.


Kod SSR-a, klijent ne izvršava JavaScript. Umjesto toga, klijent renderira HTML/CSS koji je "renderirao" poslužitelj.