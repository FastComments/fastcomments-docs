SSR, или Server-Side-Rendering, — это механизм и набор соглашений, при которых приложение или контент формируется с
минимальным участием клиента.

При SSR клиент не выполняет JavaScript. Вместо этого клиент отображает HTML/CSS, которые были "отрендерены" сервером.