De regex voor hashtags is `/\B#[a-z0-9_-]+/gi` - waarbij de `#` op verzoek kan worden aangepast.

Dit betekent dat hashtags letters, cijfers, onderstrepingstekens (_), en streepjes (-) toestaan. Ze mogen geen punten (.) of spaties ( ) bevatten.