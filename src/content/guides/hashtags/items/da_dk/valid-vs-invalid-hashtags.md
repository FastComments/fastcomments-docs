Regex'en for hashtags er `/\B#[a-z0-9_-]+/gi` - hvor `#` kan tilpasses efter anmodning.

Det betyder, at hashtags tillader bogstaver, tal, underscores (_), og bindestreger (-). De kan ikke indeholde punktummer (.) eller mellemrum ( ).