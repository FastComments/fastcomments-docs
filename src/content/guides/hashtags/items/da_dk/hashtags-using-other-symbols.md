På anmodning kan symboler andre end `#` bruges.

Forskellige tagsymboler kan bruges til at linke til forskellige indholdselementer. For eksempel kunne API'en bruges til at definere hashtags med `#` symbolet
som emneelementer, men også definere poster såsom `~document_one` og `~document_two`. I dette scenarie kunne brugere bruge hashtags såvel som brugerdefinerede tags
til at henvise til andre dokumenter.