Regularni izraz za hashtage je `/\B#[a-z0-9_-]+/gi` - kjer se `#` lahko prilagodi na zahtevo.

To pomeni, da hashtagi dovoljujejo črke, številke, podčrtaje (_), in vezaje (-). Ne smejo vsebovati pike (.) ali presledkov ( ).