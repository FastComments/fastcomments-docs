Регулярний вираз для хештегів `/\B#[a-z0-9_-]+/gi` - де `#` можна налаштувати за запитом.

Це означає, що хештеги допускають літери, цифри, підкреслення (_), і дефіси (-). Вони не можуть містити крапок (.) або пробілів ( ).