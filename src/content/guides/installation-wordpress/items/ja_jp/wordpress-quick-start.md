---
これは WordPress の手順の「要点だけ（TL;DR）」版です。

1. WordPress のダッシュボードのプラグインページに移動します。`Add Plugin` をクリックします。
2. `FastComments` を検索します。
3. 当社のプラグインをインストールして有効化します。
4. 左側のナビゲーションで `FastComments` をクリックします。
5. 指示に従います。通常は数分で完了します。長くてもそれ以上かかることはほとんどありません。
   1. 数万、あるいは数百万のコメントがある場合、同期に時間がかかることがあります。

---