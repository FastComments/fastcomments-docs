---
[related-parameter-start name = 'imageContentProfanityLevel'; type = 'string'; related-parameter-end]

預設情況下，FastComments 評論小工具的圖像內容審核已設定為 `off`。

可用的選項有 `off`、`low`、`medium` 和 `high`。

在小工具自訂 UI 中，只要未勾選 `Disable Image Uploads?`，您就可以在 `Image Content Moderation Level` 下找到此設定。

[app-screenshot-start url='/auth/my-account/customize-widget/new'; selector = '.image-profanity-level'; alt='小工具自訂頁面上的 Image Content Moderation Level 下拉選單，設定為 off、low、medium 或 high'; title='設定 Image Content Moderation Level' app-screenshot-end]