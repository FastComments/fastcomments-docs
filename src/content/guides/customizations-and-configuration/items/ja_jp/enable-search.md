---
[related-parameter-start name = 'enableSearch'; type = 'boolean'; related-parameter-end]

デフォルトでは、コメントウィジェットに検索ボックスは表示されません。

ただし、**enableSearch** フラグを true に設定することでこれを有効にできます：

[code-example-start config = {enableSearch: true}; linesToHighlight = [6]; title = '検索の有効化'; code-example-end]

これはコードなしでも行えます。ウィジェットのカスタマイズページで「Enable Search Box」オプションを確認してください。

[app-screenshot-start url='/auth/my-account/customize-widget/new'; clickSelectors = ['.enable-search-label']; selector = '.enable-search-label'; alt='ウィジェットのカスタマイズページで、検索ボックスを表示するために Enable Search Box チェックボックスがチェックされた状態'; title='検索ボックスを有効にする' app-screenshot-end]
---