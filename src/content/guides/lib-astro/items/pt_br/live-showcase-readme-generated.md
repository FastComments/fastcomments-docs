Para ver todos os widgets e fluxos sendo executados localmente contra o tenant público `demo`, clone o repositório e execute:

```bash
cd example
npm install
npm run dev
```

Cada componente tem sua própria página em `example/src/pages/` que você pode copiar diretamente para o seu próprio site Astro.