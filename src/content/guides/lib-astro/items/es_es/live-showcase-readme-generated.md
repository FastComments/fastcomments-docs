Para ver todos los widgets y flujos ejecutándose localmente contra el tenant público `demo`, clona el repositorio y ejecuta:

```bash
cd example
npm install
npm run dev
```

Cada componente tiene su propia página en `example/src/pages/` que puedes copiar directamente en tu propio sitio Astro.