Pour voir chaque widget et flux s'exécutant localement contre le tenant public `demo`, clonez le dépôt et lancez :

```bash
cd example
npm install
npm run dev
```

Chaque composant a sa propre page sous `example/src/pages/` que vous pouvez copier directement dans votre propre site Astro.