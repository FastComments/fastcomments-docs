Um jedes Widget und jeden Flow lokal gegen den öffentlichen `demo`-Tenant auszuführen, klone das Repo und führe aus:

```bash
cd example
npm install
npm run dev
```

Jede Komponente hat ihre eigene Seite unter `example/src/pages/`, die du direkt in deine eigene Astro-Website kopieren kannst.