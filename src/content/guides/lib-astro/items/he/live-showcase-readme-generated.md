כדי לראות כל ווידג'ט וכל זרימה פועלים מקומית מול ה-tenant הציבורי `demo`, שכפל את המאגר והריץ:

```bash
cd example
npm install
npm run dev
```

לכל רכיב יש דף משלו תחת `example/src/pages/` שאתה יכול להעתיק ישירות לאתר Astro שלך.