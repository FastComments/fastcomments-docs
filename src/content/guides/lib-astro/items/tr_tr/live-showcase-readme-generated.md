Kamuya açık `demo` kiracına karşı yerel olarak çalışan tüm widget'ları ve akışları görmek için repo'yu klonlayın ve çalıştırın:

```bash
cd example
npm install
npm run dev
```

Her bileşenin, kendi Astro sitenize doğrudan kopyalayabileceğiniz `example/src/pages/` altında ayrı bir sayfası vardır.