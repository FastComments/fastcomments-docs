For at se alle widgets og flows, der kører lokalt mod den offentlige `demo` tenant, klon repoet og kør:

```bash
cd example
npm install
npm run dev
```

Hver komponent har sin egen side under `example/src/pages/`, som du kan kopiere direkte ind i dit eget Astro-site.