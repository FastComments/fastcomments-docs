---
パブリックな `demo` テナントに対して動作しているすべてのウィジェットとフローをローカルで確認するには、リポジトリをクローンして次を実行してください:

```bash
cd example
npm install
npm run dev
```

各コンポーネントは `example/src/pages/` 配下にそれぞれページがあり、これらをそのまま自分の Astro サイトにコピーできます。
---