Per vedere ogni widget e flow in esecuzione localmente contro il tenant pubblico `demo`, clona il repository ed esegui:

```bash
cd example
npm install
npm run dev
```

Ogni componente ha la propria pagina sotto `example/src/pages/` che puoi copiare direttamente nel tuo sito Astro.