Da biste vidjeli sve widgete i tokove koji se pokreću lokalno u odnosu na javni `demo` tenant, klonirajte repozitorij i pokrenite:

```bash
cd example
npm install
npm run dev
```

Svaka komponenta ima svoju stranicu u `example/src/pages/` koju možete izravno kopirati u vlastitu Astro stranicu.