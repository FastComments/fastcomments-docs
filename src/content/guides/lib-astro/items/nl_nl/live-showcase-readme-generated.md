Om alle widgets en flows lokaal te zien draaien tegen de openbare `demo` tenant, kloon de repo en voer uit:

```bash
cd example
npm install
npm run dev
```

Elk component heeft zijn eigen pagina onder `example/src/pages/` die je rechtstreeks in je eigen Astro-site kunt kopiëren.