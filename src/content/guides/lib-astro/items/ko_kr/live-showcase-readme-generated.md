공용 `demo` 테넌트를 대상으로 로컬에서 모든 위젯과 플로우가 실행되는 것을 보려면, 저장소를 클론하고 다음을 실행하세요:

```bash
cd example
npm install
npm run dev
```

각 컴포넌트는 `example/src/pages/` 아래에 자체 페이지를 가지고 있으며, 이 페이지들을 그대로 복사하여 자신의 Astro 사이트에 넣을 수 있습니다.