---
SDKインスタンスの使用が終わったら（例: ビューが閉じられるとき）、WebSocket接続を閉じ、バックグラウンドタスクをキャンセルするために`cleanup()`を呼び出してください:

```swift
sdk.cleanup()
```

SwiftUI の `@StateObject` によって管理されているビューでは、通常これは `.onDisappear` 内、またはビューが解放されるときに呼び出されます。

---
---