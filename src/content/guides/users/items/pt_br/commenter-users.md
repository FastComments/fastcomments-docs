`Commenters` não pertencem a nenhum tenant. `SSO Commenters` são criados automaticamente como parte da integração SSO.

`SSO Commenters` podem receber permissões de administrador. Se o e-mail deles corresponder ao e-mail de um
administrador existente, as permissões serão as mesmas.