`Commenters` do not belong to any tenant. `SSO Commenters` are created automatically as part of the SSO integration.

`SSO Commenters` can be granted administrator permissions. If their email matches that of an
existing administrator, they will have the same permissions.