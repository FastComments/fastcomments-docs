`Moderators` and `Administrators` can be added to your tenant.

By default, when they comment, these users will have `Moderator` and `Administrator` labels next to their comments, respectively.

Administrators can be given granular, role-based permissions (RBAC), for example, to manage other administrators.