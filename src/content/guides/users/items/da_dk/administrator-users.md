`Moderators` og `Administrators` kan tilføjes til din tenant.

Som standard vil disse brugere, når de kommenterer, have etiketterne `Moderator` og `Administrator` ved siden af deres kommentarer, henholdsvis.

Administratorer kan tildeles granulære, rollebaserede tilladelser (RBAC), for eksempel til at administrere andre administratorer.