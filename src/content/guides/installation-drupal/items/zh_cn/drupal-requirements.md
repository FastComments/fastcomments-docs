---
- Drupal 10 或 11
- PHP 8.1 或更高版本
- 一个 [FastComments](https://fastcomments.com) 帐户

该模块不需要任何其他 Drupal 模块或第三方库。它仅依赖于 Drupal 核心 (`user`, `field`)。

A `<noscript>` 回退为没有启用 JavaScript 的访客提供服务器渲染的评论，因此 Live Comments 和 Streaming Chat 在禁用脚本的浏览器中仍然可用。

---