---
テーマをエディタで開いた状態で、`package.json` を開き、`name` フィールドを別の値に変更してください。今回は末尾に `-fastcomments` を追加します:

<div class="screenshot white-bg">
    <div class="title">テーマ名を変更</div>
    <img class="screenshot-image" src="/images/installation-guides/ghost-step-3-1-name-theme.png" alt="テーマ名を変更" />
</div>

IDE を使用している場合、この変更は自動的に保存されます。

---