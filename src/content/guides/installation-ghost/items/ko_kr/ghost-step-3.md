---
테마를 에디터에서 연 상태에서, `package.json`을 열고 `name` 필드를 다른 값으로 변경하세요. 여기서는 끝에 `-fastcomments`를 추가합니다:

<div class="screenshot white-bg">
    <div class="title">테마 이름 바꾸기</div>
    <img class="screenshot-image" src="/images/installation-guides/ghost-step-3-1-name-theme.png" alt="테마 이름 바꾸기" />
</div>

If you're using an IDE it will auto-save after you make this change.

---