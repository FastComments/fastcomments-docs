---
공용 `demo` 테넌트에서 로컬로 실행되는 모든 위젯과 플로우를 보려면, 저장소를 복제하고 다음을 실행하세요:

```bash
cd examples/example-showcase
npm install
npm run dev
```

각 위젯/플로우마다 자체 뷰가 `examples/example-showcase/src/views/` 아래에 있으며, 이를 바로 자신의 React 앱에 복사할 수 있습니다.
---