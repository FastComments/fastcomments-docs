공개 `demo` 테넌트에 대해 로컬에서 실행 중인 모든 위젯과 플로우를 보려면, 저장소를 클론한 후 다음을 실행하세요:

```bash
cd examples/example-showcase
npm install
npm start
```

각 위젯/플로우는 `examples/example-showcase/src/views/` 아래에 각각의 뷰가 있으며, 이를 그대로 복사하여 자신의 React 앱에 붙여넣을 수 있습니다.