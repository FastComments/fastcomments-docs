---
Casos de uso individuais (modo escuro, paginação, etc.) estão em suas próprias pastas dentro de `examples/`. Cada um pode ser configurado com `npm install` e iniciado com `npm run dev` (Vite).
---