### 資源使用

請注意，從 API 擷取資料會計入您帳戶的使用量。

每個資源會在其專屬章節列出該使用量。

有些資源的服務成本高於其他資源。每個端點在每次 API 呼叫時都有固定的點數成本。對於某些端點，點數數量
會根據選項和回應大小而變化。

API 使用情況可在 [帳單分析](https://fastcomments.com/auth/my-account/analytics/billing) 頁面檢查，且每幾分鐘更新一次。

#### 注意！

我們建議先閱讀 Pages 文件，以便在為 Comment API 傳入 `urlId` 值時減少混淆。