A **триггер** — это событие, которое будит агента. У каждого агента может быть определено один или несколько триггеров.

### The full list

| Триггер | Когда срабатывает |
|---|---|
| [Комментарий добавлен](#trigger-comment-add) | Добавлен новый комментарий. |
| [Комментарий отредактирован](#trigger-comment-edit) | Комментарий отредактирован. Предыдущий текст включён в контекст агента. |
| [Комментарий удалён](#trigger-comment-delete) | Комментарий удалён. |
| [Комментарий закреплён](#trigger-comment-pin) | Комментарий закреплён (кем угодно, включая модератора или другого агента). |
| [Комментарий откреплён](#trigger-comment-unpin) | Комментарий откреплён. |
| [Комментарий заблокирован](#trigger-comment-lock) | Комментарий заблокирован (дальнейшие ответы не разрешены). |
| [Комментарий разблокирован](#trigger-comment-unlock) | Комментарий разблокирован. |
| [Комментарий пересёк порог голосов](#trigger-comment-vote-threshold) | Суммарные голоса комментария достигают настроенного порога. |
| [Комментарий пересёк порог флагов](#trigger-comment-flag-threshold) | Количество флагов комментария достигает ровно настроенного порога. |
| [Пользователь публикует первый комментарий](#trigger-new-user-first-comment) | Пользователь публикует свой первый комментарий на этом сайте. |
| [Комментарий автоматически помечен как спам](#trigger-comment-auto-spammed) | Комментарий автоматически помечается как спам механизмом обнаружения спама. |
| [Модератор пометил комментарий как просмотренный](#trigger-moderator-reviewed) | Модератор отмечает комментарий как просмотренный. |
| [Модератор одобрил комментарий](#trigger-moderator-approved) | Модератор одобряет комментарий. |
| [Модератор пометил как спам](#trigger-moderator-spammed) | Модератор помечает комментарий как спам. |
| [Модератор присудил бейдж](#trigger-moderator-awarded-badge) | Модератор присуждает пользователю бейдж. |

### Несколько триггеров у агента

Агент может подписаться на любую комбинацию триггеров — например, [шаблон модератора](#template-moderator) подписывается как на `COMMENT_ADD`, так и на `COMMENT_FLAG_THRESHOLD`. Каждый из событий запускает агента один раз с контекстом этого события.

### Что предотвращает срабатывание агента

Подписанное событие триггера **не** запускает агента, если выполняется любое из следующих условий:

- Статус агента [статус](#status-states) — **Отключён**.
- [Область URL или локали](#scope-url-locale) агента не соответствует комментарию, вызвавшему триггер.
- [Дневной, месячный или лимит скорости бюджета](#budgets-overview) агента исчерпан — триггер фиксируется как **отброшено** с указанием причины. См. [Причины отбрасывания](#drop-reasons).
- Конкурентность для этого агента исчерпана (ограничено на агента).
- У тенанта агента недействителен биллинг.
- Действие, вызвавшее триггер, было выполнено ботом или другим агентом (предотвращение циклов).
- Триггер относится к комментарию, который уже был обработан этим агентом в пределах окна дедупликации.

Когда подписанный триггер срабатывает успешно, в [Истории запусков](#run-history) агента появляется строка со статусом **Запущен**, которая при завершении выполнения переходит в **Успешно** или **Ошибка**.

### Пороги голосов и флагов

Два триггера — **Comment Crosses Vote Threshold** и **Comment Crosses Flag Threshold** — требуют числового порога в форме редактирования. Триггер срабатывает в момент, когда счёт достигает заданного значения (конкретно, триггер порога флагов срабатывает, когда `flagCount === flagThreshold`, поэтому выбор 1 означает «сработать при первом флаге», а выбор 5 — «сработать при поступлении пятого флага»).

### Отложенные триггеры

Любой триггер можно отложить, чтобы агент выполнился позже, например после того, как голоса/флаги/ответы устаканятся. См. [Отложенные триггеры](#trigger-deferred-delay).

### Предотвращение циклов

Чтобы предотвратить бесконечные циклы, комментарии, **написанные агентом**, несут в себе `botId`. Триггеры на новые комментарии игнорируют комментарии с `botId`.

Итог: агенты могут реагировать на *действия людей* в вашем тенанте, но действия, сгенерированные агентами, никогда не вызывают триггеры агентов. Это применяется ко всем типам триггеров.

### REPLAY: внутренний триггер

Также существует внутренний тип триггера `REPLAY`, используемый функцией [Тестовые запуски (повторы)](#test-runs-replays). Вы не можете выбрать его в форме редактирования — он существует для того, чтобы серии воспроизведений помечались отдельно в истории запусков и исключались из просмотров живых запусков.