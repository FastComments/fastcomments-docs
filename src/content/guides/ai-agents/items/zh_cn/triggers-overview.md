A **触发器** 是唤醒代理的事件。每个代理可以定义一个或多个触发器。

### 完整列表

| Trigger | When it fires |
|---|---|
| [评论已添加](#trigger-comment-add) | 发布一条新评论。 |
| [评论已编辑](#trigger-comment-edit) | 评论被编辑。先前的文本包含在代理的上下文中。 |
| [评论已删除](#trigger-comment-delete) | 评论被删除。 |
| [评论已置顶](#trigger-comment-pin) | 评论被置顶（任何人，包括版主或另一个代理）。 |
| [评论已取消置顶](#trigger-comment-unpin) | 评论被取消置顶。 |
| [评论已锁定](#trigger-comment-lock) | 评论被锁定（不允许进一步回复）。 |
| [评论已解锁](#trigger-comment-unlock) | 评论被解锁。 |
| [评论达到投票阈值](#trigger-comment-vote-threshold) | 评论的净投票数达到配置的阈值。 |
| [评论达到标记阈值](#trigger-comment-flag-threshold) | 评论的标记计数恰好达到配置的阈值。 |
| [用户发布首条评论](#trigger-new-user-first-comment) | 用户在此站点发布他们的第一条评论。 |
| [评论被自动标记为垃圾](#trigger-comment-auto-spammed) | 评论被垃圾引擎自动标记为垃圾。 |
| [版主审核了评论](#trigger-moderator-reviewed) | 版主将评论标记为已审核。 |
| [版主批准了评论](#trigger-moderator-approved) | 版主批准了一条评论。 |
| [版主标记为垃圾](#trigger-moderator-spammed) | 版主将评论标记为垃圾。 |
| [版主授予徽章](#trigger-moderator-awarded-badge) | 版主向用户授予了徽章。 |

### 每个代理的多个触发器

代理可以订阅任何组合的触发器——例如，[版主模板](#template-moderator) 同时订阅了 `COMMENT_ADD` 和 `COMMENT_FLAG_THRESHOLD`。每个事件都会以该事件的上下文触发代理一次。

### 什么会阻止代理触发

如果满足以下任一情况，已订阅的触发事件**不会**触发代理：

- 代理的 [状态](#status-states) 为 **已禁用**。
- 代理的 [URL 或 区域范围](#scope-url-locale) 与触发评论不匹配。
- 代理的 [每日、每月或速率限制预算](#budgets-overview) 用尽——触发器会被记录为 **已丢弃** 并给出原因。参见 [丢弃原因](#drop-reasons)。
- 此代理的并发已饱和（按代理设置上限）。
- 代理所在租户的计费无效。
- 触发该操作的行为本身是由机器人或另一个代理执行（用于防止循环）。
- 触发针对的评论已在去重窗口内被此代理处理过。

当已订阅的触发成功触发时，代理的 [运行历史](#run-history) 显示一行状态为 **已开始** 的记录，该记录在运行完成时会变为 **成功** 或 **错误**。

### 投票和标记阈值

两个触发器——**评论达到投票阈值** 和 **评论达到标记阈值**——在编辑表单上需要一个数值阈值。触发器在计数越过配置值的那一刻触发（具体而言，标记阈值触发器在 `flagCount === flagThreshold` 时触发，因此选择 1 表示“在第一次标记时触发”，选择 5 表示“在第五次标记到来时触发”）。

### 延迟触发器

任何触发器都可以被延迟，以便代理稍后运行，例如在投票/标记/回复有时间稳定之后运行。参见 [延迟触发器](#trigger-deferred-delay)。

### 防止循环

为了防止无限循环，**由代理撰写的评论** 会携带一个 `botId`。新评论触发会忽略带有 `botId` 的评论。

最终效果：代理可以对租户中的 *人为* 操作做出反应，但代理来源的操作永远不会触发任何代理触发器。此规则适用于所有触发器类型。

### REPLAY：内部触发器

还有一种内部 `REPLAY` 触发类型，由 [测试运行（重放）](#test-runs-replays) 功能使用。您不能在编辑表单上选择它——它的存在是为了使重放运行在运行历史中被明确标记，并在实时运行视图中被排除。

---