A **триггер** — это событие, которое «будит» агента. У каждого агента может быть определён один или несколько триггеров.

### Полный список

| Trigger | When it fires |
|---|---|
| [Комментарий добавлен](#trigger-comment-add) | Появляется новый комментарий. |
| [Комментарий отредактирован](#trigger-comment-edit) | Комментарий отредактирован. Предыдущий текст включён в контекст агента. |
| [Комментарий удалён](#trigger-comment-delete) | Комментарий удалён. |
| [Комментарий закреплён](#trigger-comment-pin) | Комментарий закреплён (кем угодно, включая модератора или другого агента). |
| [Комментарий откреплён](#trigger-comment-unpin) | Комментарий откреплён. |
| [Комментарий заблокирован](#trigger-comment-lock) | Комментарий заблокирован (дальнейшие ответы запрещены). |
| [Комментарий разблокирован](#trigger-comment-unlock) | Комментарий разблокирован. |
| [Комментарий достигает порога голосов](#trigger-comment-vote-threshold) | Чистый счёт голосов комментария достигает настроенного порога. |
| [Комментарий достигает порога флагов](#trigger-comment-flag-threshold) | Количество флагов комментария достигает точно настроенного порога. |
| [Пользователь публикует первый комментарий](#trigger-new-user-first-comment) | Пользователь публикует свой первый комментарий на этом сайте. |
| [Комментарий автоматически помечен как спам](#trigger-comment-auto-spammed) | Комментарий автоматически помечен как спам системой обнаружения спама. |
| [Модератор просматривает комментарий](#trigger-moderator-reviewed) | Модератор помечает комментарий как просмотренный. |
| [Модератор одобряет комментарий](#trigger-moderator-approved) | Модератор утверждает комментарий. |
| [Модератор помечает как спам](#trigger-moderator-spammed) | Модератор помечает комментарий как спам. |
| [Модератор присуждает бейдж](#trigger-moderator-awarded-badge) | Модератор присуждает пользователю бейдж. |

### Несколько триггеров на агента

Агент может подписываться на любую комбинацию триггеров — например, [Шаблон модератора](#template-moderator) подписывается и на `COMMENT_ADD`, и на `COMMENT_FLAG_THRESHOLD`. Каждое событие запускает агента один раз с контекстом этого события.

### Что мешает срабатыванию агента

Подписанное событие триггера **не** запускает агента, если выполняется любое из следующих условий:

- [статус](#status-states) агента — **Disabled**.
- Область действия агента по [URL или локали](#scope-url-locale) не соответствует триггерному комментарию.
- Суточный, месячный или лимитный бюджет агента ([daily, monthly, or rate-limit budget](#budgets-overview)) исчерпан — триггер регистрируется как **dropped** с указанием причины. См. [Причины отброса](#drop-reasons).
- Параллельность для этого агента исчерпана (установлен предел на агента).
- У tenant'а агента некорректная платёжная информация.
- Действие, вызвавшее триггер, было выполнено ботом или другим агентом (предотвращение зацикливания).
- Триггер относился к комментарию, который уже был обработан этим агентом в пределах окна дедупликации.

Когда подписанный триггер успешно срабатывает, в [Истории запусков](#run-history) агента появляется строка со статусом **Started**, которая при завершении запуска меняется на **Success** или **Error**.

### Пороги голосов и флагов

Два триггера — **Комментарий достигает порога голосов** и **Комментарий достигает порога флагов** — требуют числового порога в форме редактирования. Триггер срабатывает в тот момент, когда счёт пересекает настроенное значение (конкретно, триггер порога флагов срабатывает, когда `flagCount === flagThreshold`, так что выбор 1 означает «сработать при первом флаге», а выбор 5 — «сработать при поступлении пятого флага»).

### Отложенные триггеры

Любой триггер можно отложить, чтобы агент выполнился позже, например после того, как голоса/флаги/ответы успеют стабилизироваться. См. [Отложенные триггеры](#trigger-deferred-delay).

### Предотвращение зацикливания

Чтобы предотвратить бесконечные циклы, комментарии, **созданные агентом**, содержат `botId`. Триггеры новых комментариев игнорируют комментарии с `botId`.

Итог: агенты могут реагировать на *человеческие* действия в вашем tenant'е, но действия, инициированные агентами, никогда не запускают триггеры агентов. Это относится ко всем типам триггеров.

### REPLAY: внутренний триггер

Существует также внутренний тип триггера `REPLAY`, используемый в функции [Тестовые запуски (повторы)](#test-runs-replays). Вы не можете выбрать его в форме редактирования — он нужен для того, чтобы повторы помечались отдельно в истории запусков и исключались из представлений живых запусков.