---
Le type de badge `Comment Count` est attribué en fonction d'un seuil donné. Il n'est attribué que sur la base des `approved comments`.

Note : la suppression de commentaires n'entraîne pas la suppression de ce badge.

---