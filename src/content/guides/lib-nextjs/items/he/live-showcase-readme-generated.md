כדי לראות כל רכיב וזרימה פועלים מקומית מול ה-`demo` tenant הציבורי, שכפל את המאגר והרץ:

```bash
cd example
npm install
npm run dev
```

לכל רכיב יש נתיב משלו תחת `example/app/` שניתן להעתיק ישירות לאפליקציית Next.js שלך.