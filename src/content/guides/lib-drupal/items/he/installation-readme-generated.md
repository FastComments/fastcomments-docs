1. מקם את המודול הזה בתיקיית `modules/custom/fastcomments/` של אתר ה-Drupal שלך (או התקן באמצעות Composer: `composer require drupal/fcom`).
2. הפעל את המודול:
   ```bash
   drush en fastcomments
   ```
   או הפעל דרך ממשק הניהול ב-**הרחבות** (`/admin/modules`).