1. 이 모듈을 Drupal 사이트의 `modules/custom/fastcomments/` 디렉터리 (또는 Composer로 설치: `composer require drupal/fcom`).
2. 모듈을 활성화하세요:
   ```bash
   drush en fastcomments
   ```
   또는 관리 UI의 **Extend** (`/admin/modules`)에서 활성화하세요.