---
1. Coloque este módulo no diretório `modules/custom/fastcomments/` do seu site Drupal (ou instale via Composer: `composer require drupal/fcom`).
2. Ative o módulo:
   ```bash
   drush en fastcomments
   ```
   Ou ative via a interface de administração em **Extend** (`/admin/modules`).
---