1. Postavite ovaj modul u direktorijum vaše Drupal stranice `modules/custom/fastcomments/` (ili instalirajte putem Composera: `composer require drupal/fcom`).
2. Omogućite modul:
   ```bash
   drush en fastcomments
   ```
   Ili omogućite preko administratorskog UI na **Extend** (`/admin/modules`).