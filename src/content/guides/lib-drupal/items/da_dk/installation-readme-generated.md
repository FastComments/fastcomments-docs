---
1. Placer dette modul i mappen `modules/custom/fastcomments/` på din Drupal-side (eller installer via Composer: `composer require drupal/fcom`).
2. Aktivér modulet:
   ```bash
   drush en fastcomments
   ```
   Eller aktivér via admin-UI'en under **Udvid** (`/admin/modules`).
---